# Panther OS

## Run
```
npm install
npm start
```
Open Chrome at http://localhost:8080

## Files
- index.html — full UI (all assets embedded)
- sw.js — Scramjet service worker
- server.js — Node.js server + Wisp proxy
- scram/ — Scramjet proxy engine
- baremux/ — BareMux transport layer
